// Create a file called index.ts
console.log("Node.js environment working correctly!");

// Test a simple TypeScript feature
interface User {
  name: string;
  age: number;
}

const user: User = {
  name: "Test User",
  age: 30
};

console.log(`User: ${user.name}, Age: ${user.age}`);
